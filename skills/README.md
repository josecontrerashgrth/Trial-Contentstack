# Skills – kickstart-next

Use **[AGENTS.md](../AGENTS.md)** for the skill index: names, links to each `SKILL.md`, and what each covers.

This directory is one folder per topic. Each folder’s `SKILL.md` has YAML frontmatter (`name`, `description`) for agent discovery.
